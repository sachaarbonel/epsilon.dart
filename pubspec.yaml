name: sigma
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com
# author: Sacha <sacha.arbonel@hotmail.fr>

environment:
  sdk: '>=2.5.0 <3.0.0'

dependencies:
 vector_math: ^2.0.8
 flutter:
    sdk: flutter

dev_dependencies:
  pedantic: ^1.8.0
  flutter_test:
    sdk: flutter
